export const addDriver = async (req, body) => {
  try {
    const { data } = req.body;
  } catch (error) {
    console.log(error)
  }
}

export const editDriver = async (req, res) => {
  try {
    const { data } = req.body;
  } catch (error) {
    console.log(error);
  }
}

export const deleteDriver = async (req, res) => {
  try {
    const { data } = req.body;
  } catch (error) {
    console.log(error)
  }
}

export const updateDriver = async (req, res) => {
  try {
    const { data } = req.body;
  } catch (error) {
    console.log(error)
  }
}